#include <stdio.h>
#include <math.h>

double main() {
    double x = 2.0;
    double y = 8.0;
    double pow_result = pow(x, y);
    printf("x = %f, y = %f, pow_result = %f\n", x, y, pow_result);
    return pow_result;
}